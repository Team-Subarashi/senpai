// create App function
module.exports = function (app) {
  var users = require("./controller");
  // users Routes
  // get and post request for /users endpoints
  // app.route("/users").get(users.listAllUsers).post(users.createNewUser);
  // put and delete request for /users endpoints
  //   app.route("/user/:id").put(users.updateUser).delete(users.deleteUser);
};
