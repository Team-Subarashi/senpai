import { createTheme } from '@material-ui/core'

const theme = createTheme({
  palette: {
    primary: {
      main: '#673AB7'
    },
    secondary: {
      main: '#EEEEEE'
    },
    type: 'dark'
  },
})

export default theme;